import ProductDetail from '@/components/ProductDetail';

export default function ProductPage({ params }) {
  // In a real app, fetch product data using params.id
  return <ProductDetail product={null} />;
}

