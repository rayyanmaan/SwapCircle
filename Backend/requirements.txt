fastapi>=0.95.0
uvicorn[standard]>=0.21.0
# Allow pydantic v1 or v2 - FastAPI supports both, and config.py handles both
pydantic>=1.7.4,<3.0.0
pydantic-settings>=2.0.0  # For pydantic v2 BaseSettings support (only needed if using pydantic v2)
python-dotenv>=1.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.23.0
motor>=3.1.1        # async MongoDB driver
email-validator
passlib[bcrypt]>=1.7.4
firebase-admin>=6.0.0
python-multipart
