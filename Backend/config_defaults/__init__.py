"""Centralized configuration defaults and settings.

Use this package for backend configuration (CORS, URLs, settings). Legacy
imports from config.py continue to work, but new code should import from
config_defaults.* directly to keep a single source of truth.
"""
